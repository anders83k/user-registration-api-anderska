name: First workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

        -name: Say hello
        run: echo "Hello from GitHub Actions!" #Skriver en synlig rad i loggen
